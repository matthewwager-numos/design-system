{
  "name": "@numosai/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Design tokens — CSS variables and TypeScript exports.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./tokens.css": "./src/tokens.css"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo 'tokens are source-only for now' && exit 0",
    "lint": "echo 'no lint' && exit 0",
    "clean": "rm -rf dist .turbo"
  }
}
