// Settings for this app isn't designed yet either — same honest
// placeholder as History, rather than guessing at a settings layout the
// screenshot this app was built from never showed.
export function SettingsTab() {
  return (
    <div className="page">
      <p style={{ margin: 0, color: "var(--content-subtle)" }}>Settings isn't designed yet — this tab is a placeholder.</p>
    </div>
  );
}
