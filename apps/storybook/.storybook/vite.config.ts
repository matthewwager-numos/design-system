// Intentionally unused.
//
// Storybook (`@storybook/react-vite`) ignores any standalone vite.config.ts
// in this directory — Vite plugins must be registered via `viteFinal` in
// `main.ts` instead. See `./main.ts` for the actual Tailwind setup.
//
// Kept as an empty file so the import path doesn't 404 if anything older
// references it. Safe to delete.
export {};
